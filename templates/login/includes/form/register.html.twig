 {{ form_start(form) }}
 
<div class="form-group">
     {{ form_row(form.username, {'attr': {'minlength': 3}}) }}
</div>

<div class="form-group">
    {{ form_row(form.email, {'attr': {'minlength': 3}}) }}
</div>

<div class="form-group">
    {{ form_row(form.plainPassword.first, {'attr': {'minlength': 6}}) }}
</div>

<div class="form-group">
    {{ form_row(form.plainPassword.second, {'attr': {'minlength': 6}}) }}
</div>

<div class="form-group">
    {{ form_row(form.birthday, {'attr': {'minlength': 6}}) }}
</div>

<div class="form-group">
    <div class="custom-control custom-switch">
            {{ form_widget(form.agreeTerms, {'attr': {'class': 'custom-control-input'}, 'id': 'formCheck-1'}) }}
    </div>
</div>

{{ include ('src/alert.html.twig') }}
                        
    <div class="col text-center align-self-center my-auto">
        <button class="btn btn-danger text-monospace shadow mt-2 btnLogin" data-bs-hover-animate="pulse" id="submitBtn" type="submit" disabled>{{ data.btn }}</button>
    </div>
    
{{ form_end(form) }}